<ul>
  <% @tweet.errors.full_messages.each do |full_message| %>
    <li><%= full_message %></li>
  
  <% end %>
</ul>
<%= form_for(@tweet, html: { role: 'form' }) do |f| %>
  <div class="form-group">
  
    <%= f.text_area :content, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-lg btn-primary", :value => "Create Task"  %>
  </div>
<% end %>